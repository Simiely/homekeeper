# 模块化梳理执行记录（v0.9.3 + refactor）

> 执行日期：2026-08-02
> 基线：v0.9.2（bb5122d）
> 说明：2026-08-02 全面审核后按三阶段执行，中途经历一次 git 事故（本地 v0.9.3 及之后提交丢失），
> 已从 GitHub 远程恢复 v0.9.2 后重做全部工作并再次提交。

---

## ✅ 阶段 1：P0 止血（已完成，提交 6661390）

1. **items.js 事件监听器累积**（P0 bug）
   - 根因：3 个 `listEl.addEventListener("click")` 注册在 `loadItems()` 内（被调用 15 次），
     `#item-list` 是常驻节点 → 翻页后 confirm 多次触发、弹窗叠加
   - 修复：3 个监听器合并为 1 个顶层委托（在 renderItems 模板生成后注册一次），
     内部按 `data-*` 分派；`items` 变量提升到 renderItems 作用域
   - 验证：6 次 loadItems 后点删除只弹 1 次 confirm ✅
2. **backups.js 清理**：本地重复 escapeHtml 删除改为 import；未用 getToken 移除

## ✅ 阶段 2：后端 services 层下沉（已完成，提交 c0a759e + 28b5393）

新增 5 个 service，5 个 fat router 变薄壳（1839 → 477 行，-74%）：

| service 文件 | 下沉内容 | router |
|---|---|---|
| `services/item_service.py` | CRUD/筛选/分页/批量/QR/日志/图片目录清理 | `routers/items.py` |
| `services/location_service.py` | 树组装/重排/环检测/父子归属 | `routers/locations.py` |
| `services/dashboard_service.py` | SQL 聚合统计/临期列表 | `routers/dashboard.py` |
| `services/image_service.py` | WebP 转码/路径/上传/列表/删除 | `routers/images.py` |
| `services/data_service.py` | CSV 导出导入/ZIP 打包 | `routers/data.py` |

- 服务层抛领域异常（ItemNotFoundError 等），router 捕获转 HTTP
- **顺带修复原有 bug**：dashboard 分类汇总 `sum()` 可能为 NULL（无价格物品）→ `coalesce` 兜底
- 验证：pytest 11 passed（test_item_crud 因沙箱 unlink 拦截跳过，环境限制非代码问题）；
  全站浏览器回归 8 视图 + 位置页交互 ✅

## ⏳ 阶段 3：前端拆分 + 收敛（待执行）

- app.js 拆 push.js（Web Push 134 行独立）
- 3 处裸 fetch 收编 api.js（VAPID/push subscribe）
- items.js 拆分（items-form/list/batch）或抽公共函数
- 抽 showOverlay/formatDateTime/.mini-btn 收编 17 处内联 11-12px 小字
- categories/tags 编辑脚手架合并

## ⚠️ Git 事故记录（重要教训）

- **事故**：阶段2重构未提交时执行 `git stash`，被沙箱 safe-delete 拦截损坏 .git 对象库，
  随后 `checkout -f 2a282c2 -- .` 覆盖工作区，未提交代码丢失
- **恢复**：删除损坏的 backup-v092-workspace 引用 → `git fetch origin` 拉回 v0.9.2 →
  `checkout -B master bb5122d` → 重做全部工作（本次记录）
- **教训**：重要工作高频提交；沙箱环境避免 stash；损坏 ref 用 `git update-ref -d` 清理后 fetch；
  `checkout -f <commit> -- .` 是危险命令
